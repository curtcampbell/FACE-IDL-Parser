## enum.java.vm
## Generates a Java public enum for an IDL enum.
##
## §4.14.10.8.3 — "An IDL enum maps to a Java public enum."
##
## Context:
##   $enumNode     — EnumNode
##   $packageName  — String  (e.g. "face.dm.samplemodel")
##   $java         — JavaTypeHelper
##
## Generated by Entity Reactor IDL Generator.
#if(!$packageName.isEmpty())
package ${packageName};

#end
/**
 * Java enum for IDL enum {@code ${enumNode.name}}.
 *
 * <p>FACE Technical Standard 3.2 §4.14.10.8.3
 */
public enum ${enumNode.name} {
#foreach($val in $enumNode.values)
#if($foreach.last)
    ${val.name}
#else
    ${val.name},
#end
#end
}
