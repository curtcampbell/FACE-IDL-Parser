## enum.py.vm
## Generates a Python IntEnum for an IDL enum.
## Target: Python 3.10+
##
## Context:
##   $enumNode — EnumNode
##   $py       — PythonTypeHelper
##
## Generated by Entity Reactor IDL Generator.
from enum import IntEnum


class ${enumNode.name}(IntEnum):
    """IDL enum ${enumNode.name}."""
#foreach($val in $enumNode.values)
    ${val.name} = ${foreach.index}
#end
